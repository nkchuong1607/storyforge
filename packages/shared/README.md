# @storyforge/shared

Reserved for shared TypeScript types, JSON schemas, and OpenAPI-generated clients used by both `apps/web` and `apps/api` consumers.

Empty in bootstrap phase. Add package when web and api need the same contract definitions.
