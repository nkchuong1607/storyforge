# Claude Code Skills

Symlinks to portable skills in `skills/`. Claude Code discovers skills from `.claude/skills/`.

Vendored AAS implementation-craft skills live in `vendor/aas-skills/` — see `skills/storyforge-aas-stack/SKILL.md`.

Install external UI skills separately — see `docs/agent-setup.md`.
